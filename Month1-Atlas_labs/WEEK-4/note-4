## Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot using ping/curl/dig/netstat/journalctl from the original Week 3.Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot using ping/curl/dig/netstat/journalctl from the original Week 3.Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot using ping/curl/dig/netstat/journalctl from the original Week 3.Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot using ping/curl/dig/netstat/journalctl from the original Week 3.Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot using ping/curl/dig/netstat/journalctl from the original Week 3.Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot using ping/curl/dig/netstat/journalctl from the original Week 3.Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot using ping/curl/dig/netstat/journalctl from the original Week 3.Deliberately break the deployment (stop the DB, close a port, misconfigure nginx);
troubleshoot usin
